# advntr landing page

Marketing site for **advntr** — get your trips out of the groupchat.

Static site: one `index.html`, screenshots in `img/` captured from the app
running in the iOS Simulator. No build step. Deployed on Vercel.

The main app lives in the `adventure` repo.
